/* Predloga uradnega ekipnega zapisnika (NTZS "ZAPISNIK EKIPNEGA DVOBOJA").
   Variante se razlikujejo po ligi in delijo isto ogrodje - loci ju le oznaka
   tekmovanja na obrazcu. Organizator jo izbere ob ustvarjanju/urejanju lige. */
package si.turnirko.modeli;

public enum PredlogaLige {
    /* 1. slovenska namiznoteniska liga. */
    SNTL_1,
    /* 2. in 3. slovenska namiznoteniska liga. */
    SNTL_23
}
